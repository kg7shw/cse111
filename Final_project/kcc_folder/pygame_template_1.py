# Pygmae template - skeletion for a new pygame project

import pygame
import random

WIDTH = 360
HEIGHT = 480
FPS = 30

# initialize pygame and create window
pygame.init()
pygame.mixer.init()
scree = pygame.display.set_mode((WIDTH, HEIGHT))
pygame.display.set_caption("My Game")
clock = pygame.time.Clock()

#game loop

running = True
while running:
    #process input (events)
    # update
    # Draw / Render

    screen.fill()